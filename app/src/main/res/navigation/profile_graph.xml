<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/profile_graph"
    app:startDestination="@id/profileFragment">
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.gallery2.features.profile.ProfileFragment"
        android:label="ProfileFragment">
        <action
            android:id="@+id/navigate_profileFragment_to_settingsFragment"
            app:destination="@id/settingsFragment" />
        <action
            android:id="@+id/navigate_profileFragment_to_feed_graph"
            app:destination="@id/feed_graph" />
    </fragment>
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.example.gallery2.features.settings.SettingsFragment"
        android:label="SettingsFragment" />
    <include app:graph="@navigation/feed_graph" />
</navigation>